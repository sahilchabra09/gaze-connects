# Better Auth Configuration
BETTER_AUTH_SECRET=your_secret_key_here
BETTER_AUTH_URL=http://localhost:3000

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Public HTTPS URL for environments behind a reverse proxy (Coolify, Nginx, etc.)
# Used to emit the correct WSS live preview websocket URL.
GAZECORE_PUBLIC_BASE_URL=https://your-gazecore-domain.com

# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# Redirect URI: http://localhost:3000/api/auth/callback/google (dev)
# Redirect URI: https://your-domain.com/api/auth/callback/google (prod)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Resend API Key for sending emails
RESEND_API_KEY=your_resend_api_key_here
